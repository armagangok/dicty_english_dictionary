import '../../../feature/export/export.dart';


class NounView extends StatelessWidget {
  const NounView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return ListView(
      shrinkWrap: true,
      children: const [
        Text("NounView"),
      ],
    );
  }
}
