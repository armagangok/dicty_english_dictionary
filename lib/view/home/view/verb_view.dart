import '../../../feature/export/export.dart';

class VerbView extends StatelessWidget {
  const VerbView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return ListView(
      shrinkWrap: true,
      children: const [
        Text("VerbView"),
      ],
    );
  }
}
