import "package:flutter/material.dart";

class AddingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple[50],
      child: Center(
        child: Text("SABIRLI OL!"),
      ),
    );
  }
}
